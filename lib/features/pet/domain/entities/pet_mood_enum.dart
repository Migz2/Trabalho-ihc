enum PetMood {
  ecstatic,   // felicidade >= 90
  happy,      // felicidade >= 70
  content,    // felicidade >= 50
  neutral,    // felicidade >= 30
  sad,        // felicidade >= 15
  neglected   // felicidade < 15
}
