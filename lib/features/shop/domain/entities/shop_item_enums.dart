enum ItemCategory {
  accessory,    // acessórios visuais no pet
  background,   // fundo do card do pet
  toy,          // brinquedos (bônus felicidade)
  decoration    // decoração do ambiente
}

enum ItemRarity {
  common,       // borda: sem destaque
  rare,         // borda: dourada suave
  epic          // borda: âmbar vibrante
}
